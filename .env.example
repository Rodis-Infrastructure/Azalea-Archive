# Rename this file to .env and fill in the values

# Your bot's token (get it from https://discord.com/developers/applications)
BOT_TOKEN="SECRET_TOKEN"

# Path to your database file (one will be auto-generated if it doesn't exist)
DB_PATH="./directory/file.db"

# [OPTIONAL] The URL to your webhook (get it from your Discord server's settings)
DEBUG_WEBHOOK_URL="https://discord.com/api/webhooks/CHANNEL_ID/WEBHOOK_TOKEN"